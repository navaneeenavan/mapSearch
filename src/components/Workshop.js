import React from "react";
import { FiArrowUpRight } from "react-icons/fi";

const Workshop = ({
  handleClick,
  title,
  description,
  className,
  image,
  date,
  time,
}) => {
  
};

export default Workshop;